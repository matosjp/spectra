# S.P.E.C.T.R.A. dependencies
# Install with: pip install -r requirements.txt

# --- GUI ---
ttkbootstrap
Pillow

# --- Data / numerics ---
numpy
pandas
scipy

# --- ML / stats ---
scikit-learn
statsmodels

# --- Plotting ---
matplotlib
seaborn
missingno

# --- Astronomy (isochrone/age-mass fitting) ---
madys
# madys itself depends on: astropy, h5py, and the "tap" (TAP Gaia Query) package.
# The MADYS docs recommend installing tap manually first, since a plain
# `pip install tap` grabs an unrelated package of the same name:
#   pip install git+https://github.com/mfouesneau/tap
#   pip install madys

# --- First-run data download ---
# Used to fetch the isochrone_models/ folder (Siess 2000, BHAC15 data
# tables) from Google Drive on first launch.
gdown

# --- Note ---
# tkinter is NOT pip-installable — it ships with your Python distribution
# on Windows/macOS, but on Linux you usually need the system package, e.g.:
#   sudo apt install python3-tk       (Debian/Ubuntu)
#   sudo dnf install python3-tkinter  (Fedora)
#        brew install python-tk       (MacOS)